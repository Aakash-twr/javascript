// expressions - The block of code that evaluates to a value.

// Operators 

//  + , - , *, /, % all are same as in any other programming language
// but only / operator gives the exact value not like floor or ceil value
// 7.89 (exact value)

// increment ++ and decrement -- works same as C++ language like post increment and decrement operator

4**6 // This is exponential operator that means 4*4*4*4*4*4

// let a = 44;
// console.log(a++);
// console.log(a);
// console.log(4**4);

// Assignment operators

// k=167
// k+=15
// console.log(k);

// Comparison operators

// 1.== double equal to 
// 2.=== triple equal to 
let a = 1;
let b = 1;
console.log(a==b); // gives true normal checking

a=1;
b="1"

console.log(a==b) // gives true which is wrong

// In this case strict checking is necessary so === operator is used which checks the dataType also.

console.log(a===b); // throws false

// same goes with != (not equal to) and !== (not double equal to)

// Logical Operators
// && , || (And operator and OR operator) same as Other languages

