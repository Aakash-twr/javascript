// It is a superset of javascript with additional features.
// It focus on type checking or type safety that will avoid errors in long run.
// It is just a development language which is converted into javascript and sends to the client in production.
// It act as an wrapper around javascript.


let myName:String = "Hello";

// myName = 12  throws error before compilation
console.log(myName);


// 
